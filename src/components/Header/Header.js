import React from "react";
import './Header.scss'

export default function Header({ isFavoritesShow, setIsFavoritesShow }) {
//header component ( header text can be extended )

    return (
        <header>
            <h1>FrontValue 2022</h1>
        </header>
    );
}
